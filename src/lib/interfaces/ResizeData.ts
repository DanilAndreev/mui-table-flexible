import {SystemResizeable} from "./SystemResizeable";

export interface ResizeData extends SystemResizeable{
    /**
     * systemWidth - thi width of the component, stored in table
     * @type number
     */
    systemWidth: number,
}
